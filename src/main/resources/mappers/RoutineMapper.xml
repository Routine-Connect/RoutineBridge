<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doday.dev.doday.mapper.RoutineMapper">

    <resultMap id="RoutineResultMap" type="com.doday.dev.doday.domain.Routine">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="daysOfWeek" column="days_of_week"/>
        <result property="alarmTime" column="alarm_time"/>
        <result property="isActive" column="is_active"/>
        <result property="iconId" column="icon_id"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="isAlarmEnabled" column="is_alarm_enabled"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>


    <select id="findById" resultType="com.doday.dev.doday.domain.Routine">
        SELECT * FROM Routines WHERE id = #{id}
    </select>

    <select id="findByUserIdAndDayOfWeek" resultMap="RoutineResultMap">
        SELECT * FROM Routines
        WHERE user_id = #{userId}
          AND is_active = TRUE
          AND FIND_IN_SET(#{dayOfWeek}, days_of_week) > 0
    </select>

    <select id="findAllByUserId" resultMap="RoutineResultMap">
        SELECT * FROM Routines WHERE user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO Routines (user_id, title, days_of_week, alarm_time, is_active, icon_id, sort_order, is_alarm_enabled)
        VALUES (#{userId}, #{title}, #{daysOfWeek}, #{alarmTime}, #{isActive}, #{iconId}, #{sortOrder}, #{isAlarmEnabled})
    </insert>

    <update id="update">
        UPDATE Routines
        SET title = #{title},
            days_of_week = #{daysOfWeek},
            alarm_time = #{alarmTime},
            is_active = #{isActive},
            icon_id = #{iconId},
            sort_order = #{sortOrder},
            is_alarm_enabled = #{isAlarmEnabled}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM Routines WHERE id = #{id}
    </delete>

    <select id="findTodayRoutines" resultType="com.doday.dev.doday.domain.Routine">
        SELECT * FROM Routines
        WHERE user_id = #{userId}
          AND is_active = TRUE
          AND FIND_IN_SET(#{dayOfWeek}, days_of_week) > 0
    </select>

    <select id="findByUserId" resultMap="RoutineResultMap">
        SELECT * FROM Routines
        WHERE user_id = #{userId}
        ORDER BY sort_order ASC
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM Routines WHERE user_id = #{userId}
    </select>

    <update id="updateOrder">
        UPDATE Routines
        SET sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>
</mapper>