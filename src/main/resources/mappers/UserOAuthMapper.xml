<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doday.dev.doday.mapper.UserOAuthMapper">

    <select id="findByProviderAndOauthId" resultType="com.doday.dev.doday.domain.UserOAuth">
        SELECT * FROM UserOAuth
        WHERE provider = #{provider}
          AND oauth_id = #{oauthId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO UserOAuth (user_id, provider, oauth_id)
        VALUES (#{userId}, #{provider}, #{oauthId})
    </insert>

</mapper>